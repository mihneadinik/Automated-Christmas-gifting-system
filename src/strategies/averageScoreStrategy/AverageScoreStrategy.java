package strategies.averageScoreStrategy;

public interface AverageScoreStrategy {
    /**
     * function that computes the average score
     * of a child based on his age
     * @return the average score of a certain child
     */
    Double computeAverageScore();
}
